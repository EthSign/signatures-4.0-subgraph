enum EventType {
  ContractCreated
  ContractHidden
  ContractSigningCompleted
  SignerSigned
  RecipientsAdded
}

type Event @entity {
  id: ID!
  contract: Contract!
  timestamp: BigInt!
  type: EventType!
  involvedEntity: Bytes
}

type Contract @entity {
  id: ID!
  name: Bytes!
  birth: BigInt!
  initiator: Bytes!
  signers: [Bytes!]!
  signedSigners: [Bytes!]!
  viewers: [Bytes!]!
  signed: Boolean!
}

type User @entity {
  id: ID!
  joinedTimestamp: BigInt!
}
